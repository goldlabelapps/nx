{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@11.22.0 --activate && corepack pnpm --version && corepack pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && corepack pnpm exec turbo run build --filter=nx..."
}
