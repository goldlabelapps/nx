import type { ReactNode } from "react";

export const metadata = {
  title: "Flash & ActionScript | Articles",
  description: "An overview of Flash, ActionScript, and early rich interactive web development.",
};

export default function FlashLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
