# GoldLabel Firebase Directory

This directory stores all Firebase-related files, such as rules, indexes, and configuration for the GoldLabel project.

- Place your `firestore.rules`, `firestore.indexes.json`, and other Firebase config files here.
