{
  "name": "nx",
  "version": "3.0.0",
  "packageManager": "pnpm@11.13.0",
  "engines": {
    "node": ">=20 <25"
  },
  "private": true,
  "scripts": {
    "dev": "concurrently -n nx,admin,template,nhtfs,storybook -c blue,green,yellow,cyan,magenta \"pnpm --dir apps/www dev\" \"pnpm --dir apps/admin dev\" \"pnpm --dir apps/template dev\" \"pnpm --dir apps/nhtfs dev\" \"pnpm --dir packages/design-system dev\"",
    "storybook": "pnpm --dir packages/design-system storybook",
    "build-storybook": "pnpm --dir packages/design-system build-storybook",
    "build": "pnpm --dir packages/design-system build && pnpm --dir apps/www build && pnpm --dir apps/template build && pnpm --dir apps/nhtfs build",
    "clean": "bash ./shell/clean-repo.sh",
    "lint": "turbo run lint",
    "test": "node ./tests/run-monorepo-tests.mjs",
    "test:coverage": "node ./tests/run-monorepo-coverage.mjs",
    "type-check": "turbo run type-check",
    "ci:github": "corepack enable && corepack prepare pnpm@11.13.0 --activate && corepack pnpm install --frozen-lockfile && corepack pnpm build-storybook && NEXT_TELEMETRY_DISABLED=1 corepack pnpm build",
    "ci:full": "corepack enable && corepack prepare pnpm@11.13.0 --activate && corepack pnpm install --frozen-lockfile && corepack pnpm lint && corepack pnpm type-check && corepack pnpm test && corepack pnpm build-storybook && corepack pnpm build"
  },
  "devDependencies": {
    "c8": "^10.1.3",
    "concurrently": "^9.0.1",
    "turbo": "2.10.7"
  }
}
