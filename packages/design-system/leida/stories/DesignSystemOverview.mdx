import React from "react";
import { Meta } from "@storybook/addon-docs/blocks";
import { TopBar } from "../components/navigation/TopBar.jsx";
import { Card } from "../components/surfaces/Card.jsx";
import { Eyebrow } from "../components/brand/Eyebrow.jsx";
import { Logo } from "../components/brand/Logo.jsx";
import { Badge } from "../components/feedback/Badge.jsx";
import { Tag } from "../components/feedback/Tag.jsx";
import { StepIndicator } from "../components/feedback/StepIndicator.jsx";
import { Dialog } from "../components/feedback/Dialog.jsx";
import { Button } from "../components/forms/Button.jsx";
import { Input } from "../components/forms/Input.jsx";
import { Alert } from "../components/feedback/Alert.jsx";

export const DialogPreview = () => {
  const [open, setOpen] = React.useState(false);

  return (
    <>
      <Button variant="secondary" onClick={() => setOpen(true)}>
        Open dialog example
      </Button>

      <Dialog
        open={open}
        onClose={() => setOpen(false)}
        title="Delete client note?"
        size="md"
        dismissible={true}
        backdropStyle={{
          background: "linear-gradient(180deg, rgba(26,24,20,0.14), rgba(26,24,20,0.2))",
          backdropFilter: "blur(4px)",
          WebkitBackdropFilter: "blur(4px)",
        }}
        children="A dialog body can hold longer copy, form fields, or any custom layout."
        actions={
          <>
            <Button variant="quiet" onClick={() => setOpen(false)}>
              Cancel
            </Button>
            <Button variant="primary" onClick={() => setOpen(false)}>
              Delete note
            </Button>
          </>
        }
      />
    </>
  );
};

<Meta title="Design System/Overview" />

# Leida Design System

A single page overview of the reusable primitives in this package.

<div style={{ border: "1px solid rgba(34,32,27,0.14)", borderRadius: "24px", overflow: "hidden", background: "linear-gradient(180deg, rgba(248,244,239,0.95), rgba(242,236,228,0.98))" }}>
  <TopBar
    links={[
      { label: "Brand", href: "#brand" },
      { label: "Forms", href: "#forms" },
      { label: "Feedback", href: "#feedback" },
    ]}
    cta="Book consult"
    logoHeight={28}
  />

  <div style={{ padding: "28px", display: "grid", gap: "18px" }}>
    <div style={{ display: "grid", gap: "10px", maxWidth: "64ch" }}>
      <Eyebrow tone="clay" as="block">Overview</Eyebrow>
      <DialogPreview />
      <h2 style={{ margin: 0, fontFamily: "var(--font-serif)", fontStyle: "italic", fontSize: "clamp(2rem, 4vw, 3.2rem)", lineHeight: 1 }}>
        Core components in one calm editorial surface.
      </h2>
      <div style={{ display: "flex", gap: "8px", flexWrap: "wrap" }}>
        <Badge tone="ink">React</Badge>
        <Badge tone="clay">Tokenized</Badge>
        <Badge tone="ink">Accessible</Badge>
      </div>
    </div>

    <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))", gap: "14px" }}>
      <Card variant="paper" padding="lg">
        <div style={{ display: "grid", gap: "10px" }}>
          <Logo variant="full" height={38} />
          <p style={{ margin: 0, color: "var(--leida-body)", lineHeight: 1.6 }}>
            Brand primitives and visual tokens.
          </p>
          <div style={{ display: "flex", gap: "8px", flexWrap: "wrap" }}>
            <Tag variant="clay">Brand</Tag>
            <Tag variant="outline">System</Tag>
          </div>
        </div>
      </Card>

      <Card variant="glass" padding="lg">
        <div style={{ display: "grid", gap: "10px" }}>
          <Input label="Email" placeholder="you@leida.com" />
          <Button variant="primary">Start setup</Button>
        </div>
      </Card>

      <Card variant="tile" padding="lg">
        <Alert severity="info" title="Heads up">
          Open the individual stories to explore controls and all component states.
        </Alert>
      </Card>
    </div>
  </div>
</div>

<div style={{ display: "grid", gap: "20px", marginTop: "12px" }}>
  <Card variant="paper" padding="lg">
    <div style={{ display: "grid", gap: "14px" }}>
      <Eyebrow tone="clay" as="block">Step progress</Eyebrow>
      <StepIndicator
        currentStep={1}
        steps={[
          { label: "Consult" },
          { label: "Plan" },
          { label: "Checkout" },
        ]}
      />
    </div>
  </Card>
</div>
